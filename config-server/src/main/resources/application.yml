server:
  port: 8190
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lakitha96/spring-config-store
          search-paths:
            - 'service-config/*service'
